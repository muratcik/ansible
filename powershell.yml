---
- hosts: TOWER.HOME.COM
  gather_facts: no   
  tasks:
    - name: Main Block
      block: 
        - name: Execute PowerShell script
          command: pwsh /home/ansible/execute.ps1 "{{ serverName }}" "{{ serviceName }}"
      rescue:
        - name: Debug
          debug:
            msg: "Error Occured"
          
      
